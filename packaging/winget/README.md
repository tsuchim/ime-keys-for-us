# winget Packaging

The winget package identifier is:

```text
tsuchim.ImeKeysForUS
```

The template under `tsuchim.ImeKeysForUS/0.1.0-template/` is incomplete until the GitHub Release URL and installer SHA256 are known.

